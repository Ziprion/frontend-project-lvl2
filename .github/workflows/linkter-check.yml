name: Linter-check

on:
  - push

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
    
      - name: Run linter
        run: |
          npm install
          make lint
